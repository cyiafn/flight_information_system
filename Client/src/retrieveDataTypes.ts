//import { fakeStringify } from "./fakeStringify";

export function retrieveDataTypes(data:string) {
    
  // const data = JSON.parse(variable); // Not sure if can parse but i got lazy to find alternative...
  const types = [];

  

  // const type = typeof data;
  // if (Array.isArray(data)) { // Check if the item is Array then check its subsequent items
  //   types.push("array");
  //   data.forEach((element) => getType(element));
  // } else if (type === "object") { // Check if the item is Object then check its subsequent items
  //   types.push("object");
  //   Object.values(data).forEach((val) => getType(val));
  // } else { // Check normally...
  //   types.push(type);
  // }

  //return types.toString();
}